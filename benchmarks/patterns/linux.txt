# Linux kernel benchmark patterns
# Format: name<TAB>pattern  (or just pattern on its own line)

# Literal queries
printk	printk
EXPORT_SYMBOL	EXPORT_SYMBOL
MODULE_LICENSE	MODULE_LICENSE
static_const	static const
BUG_ON	BUG_ON
WARN_ON	WARN_ON

# Identifier patterns
file_operations	struct file_operations
kobject	struct kobject
kfree	kfree(
kmalloc	kmalloc(

# Regex queries
include_linux	#include.*<linux/
init_function	static int __init \w+
module_param	module_param\w*\(
printk_level	printk\(KERN_\w+
ifdef_config	#ifdef CONFIG_\w+
func_definition	^static (int|void|long) \w+\(
